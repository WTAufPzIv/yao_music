import 'package:flutter/material.dart';

class YMusicColors {

  /// Apple Music 主品牌红
  static const Color primary = Color(0xFFFA2D48);

  /// 高亮红
  static const Color primaryLight = Color(0xFFFF375F);

  /// 按压态
  static const Color primaryDark = Color(0xFFD91F3A);

  /// 主背景
  static const Color background = Color(0xFF000000);

  /// 次背景
  static const Color secondaryBackground = Color(0xFF121212);

  /// 卡片背景
  static const Color cardBackground = Color(0xFF1C1C1E);

  /// 浮层背景
  static const Color popupBackground = Color(0xFF2C2C2E);

  /// 主文字
  static const Color textPrimary = Color(0xFFFFFFFF);

  /// 次文字
  static const Color textSecondary = Color(0xFFB3B3B3);

  /// 弱文字
  static const Color textHint = Color(0xFF7C7C80);

  /// 禁用文字
  static const Color textDisabled = Color(0xFF5A5A5F);

  /// 分割线
  static const Color divider = Color(0xFF2C2C2E);

  /// 边框
  static const Color border = Color(0xFF3A3A3C);

  /// Apple Music 未激活灰色
  static const Color grey = Colors.grey;

  /// 播放器进度条已播放
  static const Color playerProgress = primary;

  /// 播放器进度条背景
  static const Color playerProgressBackground = Color(0xFF3A3A3C);

  /// 播放器按钮
  static const Color playerIcon = Color(0xFFFFFFFF);

  /// 底部栏背景
  static const Color bottomBarBackground = Color(0xFF111111);

  /// 底部栏未选中
  static const Color bottomBarInactive = Color(0xFF8E8E93);

  /// 底部栏选中
  static const Color bottomBarActive = primary;

  /// 搜索框背景
  static const Color searchBackground = Color(0xFF2A2A2C);

  /// 遮罩层
  static const Color overlay = Color(0x88000000);

  /// 毛玻璃暗层
  static const Color blurOverlay = Color(0x66FFFFFF);
}